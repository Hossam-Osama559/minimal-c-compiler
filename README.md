a minimal c compiler that handles parts of the c language .

running the parser
put the source code in the test.txt file in the parser dir 
then compile g++ parser.cpp ./../token/token.cpp ./../lexer/lexer.cpp ./../lexer/token_detector.cpp 
and execute ./a.out
